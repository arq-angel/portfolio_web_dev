export const getHeroBackground = (theme: "light" | "dark" | "system") => {
    if (theme === "dark") {
        return "url('/images/hero-light.png')";
    }
    return "url('/images/hero-light.png')";
};
